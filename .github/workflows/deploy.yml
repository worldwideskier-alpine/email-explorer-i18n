name: Deploy to Cloudflare

# This is the only workflow that runs on a push to main. It carries the full
# check set (lint, build, worker tests) because it is the last gate before
# production -- Build now runs on pull requests only, so nothing else checks
# the commit that is about to be deployed.
on:
  push:
    branches: [main]
    # A commit that only touches these can't change what gets deployed.
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'LICENSE'
      - '.editorconfig'
  pull_request:
    branches: [main]
  workflow_dispatch: {}

# Serialize deploys to the same ref. Deliberately never cancel one in flight:
# a half-applied deploy can leave the Worker and its assets out of step.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - run: pnpm run build
      - run: pnpm test

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build-and-check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      # This deployment's own values, so a fork configures itself without
      # editing a file this repository keeps changing. Every one is optional:
      # a repository variable that was never created arrives as an empty
      # string and the checked-in default in dev/wrangler.jsonc stands. See
      # docs/deploying-your-own.md.
      WORKER_NAME: ${{ vars.WORKER_NAME }}
      R2_BUCKET_NAME: ${{ vars.R2_BUCKET_NAME }}
      VAPID_PUBLIC_KEY: ${{ vars.VAPID_PUBLIC_KEY }}
      ACCOUNT_RECOVERY_FROM: ${{ vars.ACCOUNT_RECOVERY_FROM }}
      ROOT_ADMIN_EMAIL: ${{ vars.ROOT_ADMIN_EMAIL }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v6
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # Before the build, because the build copies the dashboard into the
      # place wrangler.jsonc points at and the deploy reads the config.
      - name: Apply this deployment's configuration
        run: node packages/worker/scripts/apply-deployment-config.mjs
      - run: pnpm run build
      - name: Ensure R2 bucket exists
        working-directory: packages/worker/dev
        # Creating a bucket that is already there is an error, and an error
        # here is not a reason to stop; the deploy below fails loudly if the
        # bucket really is missing.
        run: npx wrangler r2 bucket create "${R2_BUCKET_NAME:-email-explorer-ja}" || true
      - name: Deploy Worker
        run: pnpm run deploy-dev-worker
      # RESEND_API_KEY is no longer pushed from here. It is set on the admin
      # screen instead, so rotating it does not mean editing a GitHub secret
      # and redeploying. Any Worker secret of that name already on the
      # deployment stays and is still used as a fallback, so removing this
      # step does not interrupt outbound mail; see app-settings.ts.
      - name: Set VAPID_PRIVATE_KEY secret
        working-directory: packages/worker/dev
        run: printf '%s' "$VAPID_PRIVATE_KEY" | npx wrangler secret put VAPID_PRIVATE_KEY
        env:
          VAPID_PRIVATE_KEY: ${{ secrets.VAPID_PRIVATE_KEY }}
